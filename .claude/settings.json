{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | grep '\\.rb$' | xargs bundle exec rubocop --autocorrect 2>&1",
            "timeout": 60,
            "statusMessage": "Running RuboCop..."
          }
        ]
      }
    ]
  }
}
